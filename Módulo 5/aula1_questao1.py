num1 = float(input("Digite o pimeiro número: "))
num2 = float(input("Digite o segundo número: "))

diferença = abs(num1 - num2)

diferença_arredondada = round(diferença, 2)

print ("A diferença entre os números é:", diferença_arredondada)